# cost_reclaim — feasibility check (phase 0)

**make-or-break (perf)**: cost-model page reclaim keeps recompute amplification / p99 TTFT / attention starvation bounded under KV-bound real+adversarial load. Depends on the L2 cost model.

Full spec + ideal pass bar: [`../../design.md`](../../design.md) verification gate.
