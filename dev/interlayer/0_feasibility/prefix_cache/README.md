# prefix_cache — feasibility check (phase 0)

mixed-granularity prefix cache correct + finer: ZERO correctness violations; hit length rounds to kernel_block_size not 1056.

Full spec + ideal pass bar: [`../../design.md`](../../design.md) verification gate.
