# the_win — feasibility check (phase 0)

bubble eliminated, no regression: waste -> kernel_block_size counterfactual (~1%); throughput >= stock with a real gain under KV pressure (n=3).

Full spec + ideal pass bar: [`../../design.md`](../../design.md) verification gate.
