# cuda_graph — feasibility check (phase 0)

sub-block block-table safe under CUDA-graph capture/replay across alloc/free/page-flip: ZERO replay faults, no recapture.

Full spec + ideal pass bar: [`../../design.md`](../../design.md) verification gate.
