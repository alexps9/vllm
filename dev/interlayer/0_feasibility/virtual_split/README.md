# virtual_split — feasibility check (phase 0)

attention kernel is byte-exact at kernel_block_size << page (pin the live value). Pass(ideal): bit-identical (atol=rtol=0); ksize<1056. **In progress** — see probe.py.

Full spec + ideal pass bar: [`../../design.md`](../../design.md) verification gate.
